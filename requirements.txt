streamlit==1.62.0
Authlib==1.8.0
extra-streamlit-components==0.1.81
pandas==2.3.3
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
yfinance==0.2.66
requests==2.34.2
ta==0.11.0
beautifulsoup4==4.15.0
vaderSentiment==3.3.2
groq==0.37.1
matplotlib==3.11.1
plotly==6.9.0
firebase-admin==7.5.0
python-dotenv==1.2.3
optuna==4.9.0
xgboost==3.4.1
lightgbm==4.7.0
mlflow==3.15.2

# catboost is deliberately NOT here: 330 MB, 26% of the whole install, for one
# model out of 25 that xgboost and lightgbm already cover the shape of. The app
# never imports it, so on a 1 GB deploy it is pure weight. The model-comparison
# workflow installs it separately, so Table 3.3 still covers all 25 models --
# jobs/model_comparison.py imports it behind try/ImportError and reports absent
# models rather than failing, so a local run without it degrades to 24.
